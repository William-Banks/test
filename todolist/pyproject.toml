# This project was generated with 0.3.22 using template: https://github.com/beeware/briefcase-template@v0.3.22
[tool.briefcase]
project_name = "todolist"
bundle = "com.william.todolist"
version = "0.0.1"
url = "https://todolist.william.com/todolist"
license.file = "LICENSE"
author = "William Banks"
author_email = "williambanks2007@gmail.com"

[tool.briefcase.app.todolist]
formal_name = "ToDoList"
description = "Meu gerenciador de tarefas"
long_description = """More details about the app should go here.
"""
sources = [
    "src/todolist",
]
test_sources = [
    "tests",
]

requires = [
    # Add your cross-platform app requirements here
]
test_requires = [
    # Add your cross-platform test requirements here
]

[tool.briefcase.app.todolist.macOS]
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]

[tool.briefcase.app.todolist.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.todolist.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.todolist.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.todolist.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.todolist.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.todolist.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "24.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.todolist.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.todolist.iOS]
requires = [
    # Add your iOS-specific app requirements here
]

[tool.briefcase.app.todolist.android]
requires = [
    # Add your Android-specific app requirements here
]

# Web deployments
[tool.briefcase.app.todolist.web]
requires = [
    # Add your web-specific app requirements here
]

